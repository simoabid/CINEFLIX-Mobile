import { View, Text } from "react-native";

// STUB — to be implemented (parity with web browse experience / FilterBar).
// Unified browse with multi-criteria filtering across movies + TV.
export default function BrowseScreen() {
    return (
        <View className="flex-1 bg-surface-background justify-center items-center">
            <Text className="text-white text-lg">Browse</Text>
            <Text className="text-netflix-lightgray mt-2">Coming soon</Text>
        </View>
    );
}
