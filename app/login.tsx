import { View, Text } from "react-native";

// STUB — to be implemented (parity with web src/components/auth login).
// Email/password login + Google sign-in; uses useAuth().login / googleLogin.
export default function LoginScreen() {
    return (
        <View className="flex-1 bg-surface-background justify-center items-center">
            <Text className="text-white text-lg">Log In</Text>
            <Text className="text-netflix-lightgray mt-2">Coming soon</Text>
        </View>
    );
}
